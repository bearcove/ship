# Steer input should never be disabled

The mate steer input is sometimes disabled. It should always be enabled — the user must always be able to steer, and needs to be able to focus the input to hit Esc and stop the agent.
