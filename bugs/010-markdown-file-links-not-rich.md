# Markdown file links should be rich previews

Markdown links to files (e.g. `PlanUpdateBlock.tsx`) are rendered as plain links. They should instead:

1. Show as `[filetype icon] filename`
2. On click, show a preview of the file contents
3. Include an 'Open in editor' button in the preview
