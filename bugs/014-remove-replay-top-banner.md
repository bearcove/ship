# Remove 'Connected replaying events' top banner

A "Connected replaying events [...]" banner appears at the top, plus each pane shows its own "Replaying X events" indicator. Problems:

1. There should never be a top banner — they eat up vertical space
2. There are two of them right now (one global + one per pane)
