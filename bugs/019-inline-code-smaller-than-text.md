# Inline code rendered smaller than regular text

Backticks (inline `code`) are rendered smaller than surrounding regular text in the threads. There's no reason for this — they should be the same size.
