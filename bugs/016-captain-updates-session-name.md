# Captain should update session name for session picker

Captain should be able to update the session name so the session picker shows something meaningful. This should be part of state that is regularly broadcast from the server to the browser, regardless of which session is currently being viewed.
