# Model switcher should have separate selectors for model and effort

Currently the model switcher is a single flat list of every model/effort combination (e.g. gpt-5.4/low, gpt-5.4/medium, ...). This is a long unwieldy list. It should be split into two separate selectors: one for the model and one for the effort level.
