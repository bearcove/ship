# Remove top info banner, move info to sidebar

The top banner showing `ship :: [project] :: [session]` wastes vertical space. It should be removed entirely. Important info (project, session, model selector, settings) should move to the left sidebar.

Reference: Codex's layout — sidebar shows threads/projects on the left, model and effort selectors live below the composer, no top banner at all.
