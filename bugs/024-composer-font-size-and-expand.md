# Composer text input: font size too small, should expand on focus

The text input in the composer (and other places in the UI) has font size that is too small. It should use a more readable size.

Additionally, the composer textarea should start compact (single line or small height) and expand when the user clicks/focuses it, then shrink back when empty and unfocused. This gives a cleaner default appearance without wasting vertical space.
