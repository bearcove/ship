[package]
name = "ship-server"
version = "0.1.0"
edition = "2024"

[package.metadata]

[package.metadata."docs.rs"]
rustdoc-args = ["--html-in-header", "arborium-header.html"]

[dependencies]
# TODO(dep.ship-core): add when SessionManager is implemented
# TODO(dep.ship-service): add when roam service trait is implemented
# TODO(dep.axum): add `axum` HTTP server dependency when transport layer is implemented.
# TODO(dep.figue): add `figue` for layered server configuration parsing.
