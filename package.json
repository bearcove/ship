{
  "private": true,
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "scripts": {
    "dev": "pnpm --filter ship-frontend dev",
    "build": "pnpm --filter ship-frontend build",
    "build-no-check": "pnpm --filter ship-frontend build-no-check",
    "typecheck": "pnpm --filter ship-frontend typecheck",
    "test": "pnpm --filter ship-frontend test"
  },
  "devDependencies": {
    "oxfmt": "^0.36.0",
    "oxlint": "^1.51.0"
  },
  "dependencies": {
    "gpt-tokenizer": "^3.4.0",
    "react-markdown": "^10.1.0",
    "remark-gfm": "^4.0.1"
  }
}
